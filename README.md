# OneBrain dashboard (static host)

The OneBrain dashboard shell — sign-in page + client JS. No secrets: all
configuration is fetched at runtime from the OneBrain deployment. Hosted on
GitHub Pages until the product's custom domain is attached to Supabase.
